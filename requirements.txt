# Core
anthropic>=0.40
openai>=1.40          # OpenAI-compatible providers (Groq / Cerebras / OpenRouter)
langgraph>=0.2
pyyaml>=6.0

# Analysis (Step 5+)
numpy>=1.24
scipy>=1.10
pandas>=2.0
statsmodels>=0.14.5 # Week-3 J2 mixed-effects models (§10). 0.14.5+ required for
                    # scipy>=1.15 (earlier statsmodels imports the removed scipy._lib._util._lazywhere)
matplotlib>=3.8     # Figure 2 (analysis/figures/fig2_dissociation.py); 3.8+ for
                    # deterministic PDF/SVG metadata and Pillow TIFF-LZW export

# Checks. Both were imported by tracked code without being declared here, so a clone that
# followed the README could not run either one.
sympy>=1.12         # tools/verify_problems.py re-solves domain/algebra/problems.yaml
                    # symbolically, independently of the answers recorded in the file
pytest>=8.0         # analysis/figures/test_fig2_dissociation.py is the one pytest-style
                    # test in the tree; everything under tools/ is a standalone script
